# Security

Only analyze trusted, authorized recordings under an operator-controlled root. Filenames, class names and stacks can disclose private details. Never attach sensitive JFR recordings to public issues. Report synthetic path/budget bypasses privately if reporting is enabled; otherwise request a private channel. No response-time commitment is made.
